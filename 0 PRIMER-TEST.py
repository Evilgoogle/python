def plus(x, y):
    return x + y

x = 10;
def func(*arg): # arg приходит tuple
    
    y = 15;
    sumx = arg[0] + arg[1];
    print(arg);
    
    def bn(v):
        print(x, y)
        return v - x;

    return bn(sumx);

g = lambda(): pass
print(g());
